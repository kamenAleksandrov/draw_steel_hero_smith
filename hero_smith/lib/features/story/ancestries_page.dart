import 'package:flutter/material.dart';

class AncestriesPage extends StatelessWidget {
  const AncestriesPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Ancestries')),
      body: const Center(child: Text('Ancestries (coming soon)')),
    );
  }
}
